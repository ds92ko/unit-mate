import React from 'react';
import styles from './defaultButton.module.css';

const Button = () => {
  return (
    <button className={styles.defaultButton}>Default</button>
  );
};

export default Button;
